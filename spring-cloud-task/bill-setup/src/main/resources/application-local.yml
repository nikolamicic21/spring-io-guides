spring:
  cloud:
    task:
      name: bill_setup_task
  datasource:
    url: jdbc:mysql://localhost:3306/task?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
